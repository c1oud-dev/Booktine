spring:
  datasource:
    url: jdbc:h2:mem:booktine;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
h2:
    console:
      enabled: true
      path: /h2-console